Stored Procedures

-----------------
Create User details

CREATE procedure [dbo].[CreateNewPatientRecord]

(

@Telephone nvarchar (20),
@Email nvarchar (50),
@Password (50),
@Date int,
@Address nvarchar (100),
@Security_Ans (50)

)
as
begin
Insert into Users values(@FirstName,@LastName,@Telephone,@Date,@Email,@Password,@Address,@Security_Ans)
END

----------------

Get user details

CREATE PROCEDURE [dbo].[Get_User_Detail]	
AS
BEGIN
SELECT * FROM Users
END

-------------

Delete Account

CREATE procedure [dbo].[DeletePatient]

(
@ID int
)
as
begin
Delete from Users where ID = @ID
End

-------------

Update account Details

CREATE procedure [dbo].[UpdatePatient]

(

@Fullname navchar(100),
@enail navchar(50)
@DateOfBirth int, 
@Address nvarchar (50),
@Telephone nvarchar(50),
@Email nvarchar(50)
@securityAnswer(50)

)

as

begin

Update Patients set FirstName = @FName , LastName = @LName , DateOfBirth = @DateOfBirth , Address = @Address , Telephone = @Telephone , Email=@Email
where Id=@ID End






